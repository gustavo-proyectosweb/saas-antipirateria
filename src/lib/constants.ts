// src/lib/constants.ts

// Configuración global de límites de descarga
export const DEFAULT_MAX_DOWNLOADS = 5 // Máximo N descargas por token
export const DEFAULT_EXPIRATION_DAYS = 30 // Días de validez del enlace desde el momento de la compra